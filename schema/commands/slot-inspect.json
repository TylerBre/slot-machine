{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://slot-machine/commands/slot-inspect.json",
  "title": "slot inspect",
  "description": "Deep info on one slot: branch, tracker issue, git state (dirty / commits ahead of the base), worker (live/dead), lock owner + LIVE/STALE staleness (and cross-wiring), and every PR for its branch.",
  "x-mcp": true,
  "x-web": true,
  "type": "object",
  "required": ["slot"],
  "additionalProperties": false,
  "properties": {
    "slot": { "type": "string", "description": "slot letter, e.g. \"c\"" }
  },
  "x-cli": {
    "path": ["slot", "inspect"],
    "args": {
      "slot": { "positional": 0 }
    }
  }
}
