{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://slot-machine/commands/session-attach.json",
  "title": "session attach",
  "description": "Attach/switch the client to a running session - the most recently active one by default, so a bare attach continues where you left off.",
  "x-mcp": false,
  "x-web": false,
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "session": { "type": "string", "description": "session to attach (default: the most recently active one)" }
  },
  "x-cli": {
    "path": ["session", "attach"],
    "args": {
      "session": { "positional": 0 }
    }
  }
}
