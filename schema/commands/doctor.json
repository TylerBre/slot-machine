{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://slot-machine/commands/doctor.json",
  "title": "doctor",
  "description": "Check environment + config health: tmux/git/gh/node present, gh authenticated, the resolved config and its sources, root dir, slots, and base-branch resolvability. ok=false with fails>0 means broken.",
  "x-mcp": true,
  "x-web": true,
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "fix": { "type": "boolean", "description": "repair fixable checks: bin symlinks, tmux config, MCP registration" },
    "fixTmux": { "type": "boolean", "description": "write + apply sm's tmux pane-title config only" }
  },
  "x-cli": {
    "path": ["doctor"],
    "args": {
      "fix": { "flag": "--fix", "mcpHidden": true },
      "fixTmux": { "flag": "--fix-tmux", "mcpHidden": true }
    }
  }
}
