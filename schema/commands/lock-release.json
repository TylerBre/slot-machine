{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://slot-machine/commands/lock-release.json",
  "title": "lock release",
  "description": "Free a slot worktree lock or a named resource lock (e.g. \"browser\") - the inverse of claim.",
  "x-mcp": true,
  "x-web": true,
  "type": "object",
  "required": ["name"],
  "additionalProperties": false,
  "properties": {
    "name": { "type": "string", "description": "a slot letter (free its worktree lock) or a resource name like \"browser\" (release it)" }
  },
  "x-cli": {
    "path": ["lock", "release"],
    "args": {
      "name": { "positional": 0 }
    }
  }
}
