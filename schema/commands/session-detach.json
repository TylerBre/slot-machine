{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://slot-machine/commands/session-detach.json",
  "title": "session detach",
  "description": "Detach your own client when run inside tmux with no session given; otherwise detach every client attached to the named (or sole running) session.",
  "x-mcp": false,
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "session": { "type": "string", "description": "session to detach (default: your own client if inside tmux, else the sole running session)" }
  },
  "x-cli": {
    "path": ["session", "detach"],
    "args": {
      "session": { "positional": 0 }
    }
  }
}
