{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://slot-machine/commands/floor.json",
  "title": "floor",
  "description": "One-shot fleet snapshot for the dispatcher: running sessions, every slot's worker/activity/task/lock, held resource locks, and the unread inbox count - one call instead of four. Cheap (multiplexer + lockfiles only, no git/gh); use sm_slot_ls when you need reusability classification.",
  "x-mcp": true,
  "type": "object",
  "additionalProperties": false,
  "properties": {},
  "x-cli": {
    "path": ["floor"],
    "args": {}
  }
}
