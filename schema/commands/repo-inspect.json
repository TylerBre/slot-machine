{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://slot-machine/commands/repo-inspect.json",
  "title": "repo inspect",
  "description": "One repo's resolved context (root, prefix, session prefix, base branch). Defaults to the current repo when no repo is given.",
  "x-mcp": false,
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "repo": { "type": "string", "description": "repo path or name to inspect (default: the current repo)" }
  },
  "x-cli": {
    "path": ["repo", "inspect"],
    "args": {
      "repo": { "positional": 0 }
    }
  }
}
