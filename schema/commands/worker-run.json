{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://slot-machine/commands/worker-run.json",
  "title": "worker run",
  "description": "The dispatcher hand-off: pick a free slot with a live worker (resetting a merged/reusable one to a clean base first) and send it a task. Returns the chosen slot.",
  "x-mcp": true,
  "x-web": true,
  "type": "object",
  "required": ["message"],
  "additionalProperties": false,
  "properties": {
    "message": { "type": "string", "description": "the task to send the worker" },
    "session": {
      "type": "string",
      "description": "target tmux session (optional; defaults to the sole running one)"
    },
    "brief": {
      "type": "boolean",
      "description": "prepend a one-line role pointer so the worker runs sm worker role first"
    }
  },
  "x-cli": {
    "path": ["worker", "run"],
    "args": {
      "message": { "positional": 0 },
      "session": { "flag": "--session", "short": "t" },
      "brief": { "flag": "--brief" }
    }
  }
}
